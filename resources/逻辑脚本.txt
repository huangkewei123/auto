//注释
//cut();

if( 1 ==2 ) :

	if(a == a):
		inputText(第二层);
		gotoLine(33);
	endif
	inputText(1);
elif(2 == 2) :
	inputText(2);
elif(search(搜索文字)) :
	inputText(3);
else 
	inputText(else逻辑);
endif
inputText(结束);
while( 2== 2):
    if( 1 ==g ) :
        inputText(1);
        if( 1 ==g ) :
            inputText(1);
        endif
    elif(2 == a) :
        inputText(2);
    elif(2 == 4) :
        inputText(3);
    else
        inputText(else逻辑);
    endif
edl
inputText(最后一行);